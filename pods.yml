apiVersion : v1
kind: Pod
metadata:
  name: demo-pod
  labels:
    name: demo
spec:
  containers:
  - name: demo
    image: tomcat
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080
